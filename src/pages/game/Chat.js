import React from 'react'

export default function Chat({ messages }) {
    return (
        <ul>
            {Chat.map((message) => {
                <li key={id}></li>
            })}
        </ul>
    )
}
